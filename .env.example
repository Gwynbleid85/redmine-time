# Redmine API Configuration
# Copy this file to .env and fill in your actual values

# Base URL of your Redmine instance (without trailing slash)
REDMINE_BASE_URL=https://redmine.example.com

# Optional: Predefined Issues (JSON array format)
# If not set, defaults to: [{"id":129076,"subject":"Internal activities"},{"id":185333,"subject":"Organizační meetingy"}]
# Format: JSON array of objects with "id" (number) and "subject" (string) properties
# PREDEFINED_ISSUES='[{"id":129076,"subject":"Internal activities"},{"id":185333,"subject":"Organizační meetingy"}]'

# ============================================
# Database Configuration (PostgreSQL)
# ============================================

# PostgreSQL connection URL
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/redmine_time

# ============================================
# Better Auth Configuration
# ============================================

# Base URL for authentication (should match your app URL)
# BETTER_AUTH_URL=http://localhost:3000

# Trusted origins for CORS (comma-separated)
# Add your production domain when deploying
# BETTER_AUTH_TRUSTED_ORIGINS=http://localhost:3000,http://localhost:3001,https://yourdomain.com

# Email whitelist for registration (comma-separated)
# If not set, all emails can register
# If set, only these emails can create accounts
# BETTER_AUTH_ALLOWED_EMAILS=user1@example.com,user2@example.com,admin@company.com
